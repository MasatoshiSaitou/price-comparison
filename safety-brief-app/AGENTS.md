# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Pinned to SDK 54 (not the latest) because that's what the Expo Go app
on the Play Store / App Store actually supports as of mid-2026; newer
SDKs require sideloading a per-SDK Expo Go build from expo.dev/go.
